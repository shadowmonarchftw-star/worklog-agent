"use client";
import { useState } from "react";
export function MissingDaysPanel({ history, onBackfill }) {
  const [from, setFrom] = useState(""); const [to, setTo] = useState(""); const [missing, setMissing] = useState([]);
  async function scan() { if (!from || !to || from > to) return; const dates = []; for (let d = new Date(`${from}T12:00:00`); d <= new Date(`${to}T12:00:00`); d.setDate(d.getDate() + 1)) dates.push(d.toISOString().slice(0, 10)); const response = await fetch("/api/google/check-dates", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ dates }) }); const data = response.ok ? await response.json() : { rows: {} }; setMissing(dates.filter((date) => !data.rows[date] && !history.some((entry) => entry.workDate === date))); }
  return <><p className="page-eyebrow">Backfill / Missing worklogs</p><h1>Missing worklogs</h1><p className="page-subtitle">Find dates with no local history or Google Sheet row.</p><section className="panel missing-days"><div className="missing-days-controls"><label>From<input type="date" value={from} onChange={(e) => setFrom(e.target.value)} /></label><label>To<input type="date" value={to} onChange={(e) => setTo(e.target.value)} /></label><button className="primary-action" type="button" onClick={scan}>Find missing days</button></div>{missing.length ? <div className="missing-days-result"><span>{missing.join(", ")}</span><button className="secondary-button" type="button" onClick={() => onBackfill(missing)}>Backfill missing days</button></div> : from && to ? <p className="settings-status">No missing worklogs found.</p> : null}</section></>;
}
